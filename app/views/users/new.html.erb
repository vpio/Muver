<h1>ADD YOUR PROFILE PICTURE</h1>

<%= form_with(model: @user, local: true) do |form|%>
    <div class='form-group'>
      <%= form.file_field :avatar %>
    </div>
    <div class='actions'>
      <%= form.submit 'Update My Pic! 🙃', class: 'btn btn-primary' %>
    </div>
<% end %>
