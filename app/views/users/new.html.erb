<h1>Profile Picture</h1>

<%= form_with(model: @user, local: true) do |form|%>
    <div class='form-group'>
      <%= form.file_field :avatar %>
    </div>
    <div class='actions'>
      <%= form.submit 'Update My Picture!', class: 'btn btn-primary' %>
    </div>
<% end %>
